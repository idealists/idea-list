var React = require('react');
var commentActions = require('../actions/ideaActions')


var commentList=React.createClass({

  render:function(){
    var comments = this.props.comments.map(function(commentId){
      return
    })
    
  }
})
